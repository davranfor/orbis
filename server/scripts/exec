#!/bin/bash

source "$(dirname -- "${BASH_SOURCE[0]}")/.env"

payload=$(cat << 'EOF'
SELECT json_group_array(json_array(id, name, email))
FROM users;
EOF
)

curl -s -c "$COOKIES" -b "$COOKIES" "http://$SERVER_HOST/api/exec" \
     -H "Content-Type: application/sql" \
     -d "$payload" | jq -M

