PROXY_PORT="8000"
SERVER_PORT="8001"
SERVICE_PORT="8002"
PROXY_HOST="localhost:$PROXY_PORT"
SERVER_HOST="localhost:$SERVER_PORT"
SERVICE_HOST="localhost:$SERVICE_PORT"
BASE_DIR="$(cd -- "$(dirname "${BASH_SOURCE[0]}")/.." && pwd -P)"

