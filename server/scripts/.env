PROXY_PORT="8000"
PROXY_HOST="127.0.0.1:$PROXY_PORT"
SERVER_PORT="8001"
SERVER_HOST="127.0.0.1:$SERVER_PORT"
BASE_DIR="$(cd -- "$(dirname "${BASH_SOURCE[0]}")/.." && pwd -P)"
COOKIES="$BASE_DIR/storage/cookies"

